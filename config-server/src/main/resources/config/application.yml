server:
  port: 8771
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zhuchunliu/springcloud-demo.git
          search-paths: config
#          default-label: master
  application:
    name: config-sever